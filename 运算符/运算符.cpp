
#include <iostream>
#include<cmath>
using namespace std;
int main()
{
    const double alpha = 4;
    double beta,gamma,delta,epsilon;
    beta = (alpha + 5) / 2;
    gamma = (alpha - 2) * 3; 
    delta = (alpha * 3) / 5;
    epsilon = !4;
    cout << beta << endl
        << gamma << endl
        << delta << endl
        << epsilon << endl;









    return 0;

}

